<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Danh sách buổi học của khóa học
        <%= course.name %>
    </title>
    <%- include('layouts/resources') %>
</head>

<body>
    <div class="container-fluid">
        <%- include('layouts/header') %>
        <br><br>
        <div class="row">
            <div class="col-md-3">
                <%- include('layouts/course-tasks') %>
                <div class="panel panel-primary">
                    <div class="panel-heading">Thông tin khóa học</div>
                    <div class="panel-body">
                        <ul class="nav nav-pills nav-stacked">
                            <li>Tên:
                                <%= course.name %>
                            </li>
                            <li>Mô tả:
                                <%= course.description %>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="col-md-9">
                <div class="panel panel-primary">
                    <div class="panel-heading">Danh sách buổi học của khóa học:
                        <%= course.name %>
                    </div>
                    <div class="panel-body">
                        <% if(lessons.length != 0){ var i = 1; %>
                        <% lessons.forEach(
                                        (lesson) => {
                                            %>
                        <ul class="nav nav-pills nav-stacked">
                            <li><a class="pull-left" href="/instructor/lesson-detail?lessonId=<%= lesson._id %>">Buổi
                                    <%= i %>:
                                    <%= lesson.name %> - Ngày học: <%= lesson.date %></a></li>
                        </ul>
                        <%  i++;
                                        }
                                    ) %>
                        <% }else{ %>
                        <p><i>Không có buổi học nào được hiển thị</i></p>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>